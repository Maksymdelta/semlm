import unittest


class Testing(unittest.TestCase):
    """Unit tests for semlm."""

    # Make sure we can read the main (3) types of files.
    def test_placeholder(self):
        """Don't have any tests yet.  Keeping this as a place holder."""
        assert(True)
